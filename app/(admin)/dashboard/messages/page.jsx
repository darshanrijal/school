import StudentMessagesList from "@/components/StudentMessagesList";
import React from "react";
export const metadata = {
  title: "Messages",
};
export default function Messages() {
  return <StudentMessagesList />;
}
